cat <<EOF > new_policy.yaml
constraint: constraints/cloudfunctions.allowedIngressSettings
listPolicy:
 allValues: ALLOW
EOF

gcloud resource-manager org-policies set-policy new-policy.yaml --project=data-flow-pubsub-bigtable
